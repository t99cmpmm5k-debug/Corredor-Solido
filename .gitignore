# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Backend (server/) -- credenciales reales de base de datos y JWT, nunca al repo.
server/.env
server/*.pem

# Datos reales de entrenamiento (GPS/salud de terceros) usados como
# referencia durante el desarrollo — nunca a un repo público.
*.tcx
*.fit

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
